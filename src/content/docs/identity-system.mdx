---
title: Identity System
section: Identity System
source: style-guide-pdf; section structure per Sid's brand palette overview + IA decision (15 Aug 2026)
status: approved
updated: 2026-08-15
note: Opener for the Identity System section, split from the former Identity Essentials page. Framing prose is descriptive only — every element it names comes from the style guide.
---

import { Image } from 'astro:assets';
import PhotoSet from '../../components/PhotoSet.astro';

import reelEndFramesColor from '../../assets/social-media/reel end frames old style guide example2.png';
import reelEndFramesBlack from '../../assets/social-media/reel end frames old style guide example1.png';

The identity system is everything the brand looks like before a word is
written: the wordmark, the palette, the type, the symbols. It is built from
four core elements, each with its own page, and from the expressions that
combine them — like the reel end frames below, where logo, color and tagline
meet in a single frame.

This is the last definitive section of the wiki. Everything before it
describes what Balizen is; everything after it — the design guides, the
social pages — applies what's defined here.

## The elements

- [Logo](/logo/) — the wordmark and its variations, and the artwork files
- [Color](/color/) — the core palette and how documents use it
- [Typography](/typography/) — the heading and body faces, with fallbacks
- [Symbols](/symbols/) — the symbol family and what each mark stands for

## Reel end frames

Wordmark, FTF VERIFIED mark, or "From smile to smile." — set on Balizen Teal,
Spice, Tree Hugger, or black (white text PNGs, black background as separate
shape).

<PhotoSet layout="single" label="Reel end frames color">
	<Image src={reelEndFramesColor} alt="Style guide sheet: three vertical reel end frames — the Balizen wordmark in white on Balizen Teal, the white Fair Trade Federation VERIFIED mark on Spice orange, and 'From smile to smile.' in white serif on Tree Hugger green." />
</PhotoSet>

<PhotoSet layout="single" label="Reel end frames white text pngs (black background separate shape)">
	<Image src={reelEndFramesBlack} alt="Style guide sheet: the same three reel end frames — white wordmark, white FTF VERIFIED mark, and white 'From smile to smile.' — each on a black background panel." />
</PhotoSet>
