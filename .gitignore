# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# deploy tooling (local Vercel link)
.vercel/

# local agent / editor config — machine-specific, not shared
.claude/settings.local.json

# macOS-specific files
.DS_Store

# Full-resolution photo masters. The site uses the optimised derivatives in
# src/assets/store-photos/ and src/assets/website-photos/ (328MB -> 8MB); the
# originals stay on disk for re-exporting but are too heavy to carry in git.
src/assets/store photos/
src/assets/website photos/
